
// Problem: Write a function to traverse an array and print each element.

function printArrayElements(arr) {
    for (let i = 0; i < arr.length; i++) {
        console.log(arr[i]);
    }
}

// Sample array
const array = [1, 2, 3, 4, 5];
printArrayElements(array);